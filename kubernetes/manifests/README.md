# Manifests

Raw GitOps-managed Kubernetes manifests for `homelab-core` live here.

They currently cover:

- platform integration resources such as External Secrets, cert-manager, Velero, and ingress
- Homepage configuration
- telemetry agent configuration that integrates with an external ops stack
