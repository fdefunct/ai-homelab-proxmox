#!/bin/bash
set -euo pipefail

ENV_NAME="${ENV_NAME:-home}"
REPO_ROOT=$(CDPATH='' cd -- "$(dirname -- "$0")/.." && pwd)
cd "$REPO_ROOT"

exec ./scripts/render-op inject "terraform/envs/$ENV_NAME/secrets.tfvars.template" "Terraform wrapper targets"
